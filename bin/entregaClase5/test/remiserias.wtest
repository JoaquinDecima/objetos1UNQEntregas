/* 	Test Remiserias
 *	Creado por Pato (Joaquin Decima)
 *  01 de Octubre de 2017
 */	

import remiseria.*
import remis.*

test "Dos Remiserias" {
	
	var remiseria1 = new Remiseria()
	var remiseria2 = new Remiseria()
	var cachito = new Corsa("Rojo")
	
	remiseria1.agregarAFlota(cachito)
	remiseria1.agregarAFlota(new Corsa("Negro"))
	remiseria1.agregarAFlota(new Corsa("Verde"))
	remiseria1.agregarAFlota(new Standar(true))
	remiseria1.agregarAFlota(new Remis(5, 160, "Beige",1200))
	remiseria2.agregarAFlota(cachito)
	remiseria1.agregarAFlota(new Standar(true))
	remiseria1.agregarAFlota(new Standar(false))
	remiseria1.agregarAFlota(new Standar(false))
	remiseria1.agregarAFlota(trafic)
	trafic.motorEsBaraton()
	trafic.interiorEsComodo()
	
	assert.that(true)

}